version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: codeigniter4_app
    container_name: codeigniter4_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
    networks:
      - app-network

  webserver:
    image: nginx:alpine
    container_name: codeigniter4_webserver
    restart: unless-stopped
    ports:
      - "8020:80"
    volumes:
      - ./:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
